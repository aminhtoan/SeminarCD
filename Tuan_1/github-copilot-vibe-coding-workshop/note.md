// java dotnet
docker compose -f compose.yaml up --build
chạy cỡ 3 4 phút,  thấy nó lặp lại 1 chỗ thì out ra,

fe cổng 3030

// chạy
docker compose -f compose.yaml up -d

// js py
// build
docker compose -f compose-js-py.yaml up --build

// chạy
docker compose -f compose-js-py.yaml up -d

fe cổng 3000