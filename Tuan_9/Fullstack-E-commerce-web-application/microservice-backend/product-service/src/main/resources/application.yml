spring:
  application:
    name: product-service
  config:
    import: "optional:file:.env[.properties]"
  data:
    mongodb:
      uri: ${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/product_service}
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 9010

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
